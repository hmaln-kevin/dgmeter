<%= form_with(model: load_topic, local: true) do |form| %>
  <% if load_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(load_topic.errors.count, "error") %> prohibited this load_topic from being saved:</h2>

      <ul>
      <% load_topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :pin %>
    <%= form.text_field :pin %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
